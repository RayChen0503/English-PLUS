{
  "name": "englishplus-admin-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode competition",
    "validate:competition-env": "node scripts/validate-env.mjs competition",
    "validate:production-env": "node scripts/validate-env.mjs production",
    "build": "npm run validate:competition-env && vite build --mode competition",
    "build:competition": "npm run validate:competition-env && vite build --mode competition",
    "build:production": "npm run validate:production-env && vite build --mode production",
    "preview": "vite preview",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "firebase": "12.15.0",
    "lucide": "^0.468.0"
  },
  "devDependencies": {
    "vite": "^7.2.2",
    "vitest": "^4.1.10"
  }
}
